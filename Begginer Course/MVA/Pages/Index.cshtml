@page
@model IndexModel
@{
    ViewData["Title"] = "Home page";
}

<h3>@Model.Message</h3>

<form method="post">
    <table class="table">
        <thead>
            <tr>
                <th>ID</th>
                <th>Name</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var customer in Model.Customers)
            {
                <tr>
                    <td>@customer.CustomerId</td>
                    <td>@customer.Name</td>
                    <td>
                        <a asp-page="./Edit" asp-route-id="@customer.CustomerId">Edit</a>
                        <button type="submit" asp-page-handler="delete" asp-route-id="@customer.CustomerId">Delete</button>
                    </td>
                </tr>
            }
        </tbody>
    </table>
    <a asp-page="./Create">Create</a>
</form>